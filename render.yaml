services:
  - type: web
    name: gartic-phone
    runtime: node
    buildCommand: npm run build
    startCommand: NODE_ENV=production npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
